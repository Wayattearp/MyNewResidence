export default class MarkerManager {
    constructor(map) {
        this.map = map;
        this.markers = {};
    }

    updateMarkers(houses) {
        console.log("updating house markers");
        console.log(houses)
    }
}